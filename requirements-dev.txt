# CSV 数据分析 Skill — 开发与测试依赖
# 安装: pip install -r requirements.txt -r requirements-api.txt -r requirements-dev.txt

# ========== 测试 ==========
pytest>=7.0
pytest-cov>=4.0        # 测试覆盖率

# ========== 代码质量 ==========
# bandit>=1.7          # 安全扫描
# mypy>=1.0            # 类型检查
# ruff>=0.1            # Linter

# ========== Agent Tool 支持（可选）==========
langchain-core>=0.2
